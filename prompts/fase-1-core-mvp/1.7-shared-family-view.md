# Prompt 1.7 — Shared Family View

> **Fase**: 1 (Core MVP)
> **Prasyarat**: Prompt 1.1 + 1.6 selesai (auth + dashboard)
> **Output**: Spouse viewer dashboard — read-only, filtered data

---

## Context Files
- `../AGENTS.md`, `../context/database-schema.md` (users — role), `../context/architecture.md` (Role-Based Access tabel), `../context/api-conventions.md` (Dashboard — shared-view)

---

## Prompt

```
Baca context: AGENTS.md, architecture.md (tabel Role-Based Access), api-conventions.md (shared-view endpoints).

Implementasikan Shared Family View untuk Spouse Viewer:

═══ BACKEND ═══

Middleware tambahan: SpouseDataFilter
- Inject ke setiap query: filter is_shared = true untuk assets
- Exclude: vault_references, automation_rules, audit_logs (detail)
- Spouse boleh lihat: shared assets, semua debts, bills, forecast, goals (view only), monthly reports

Endpoints (semua read-only untuk spouse):
- GET /api/v1/shared-view/summary → simplified dashboard
  - Total aset bersama, total utang, net worth bersama
  - Upcoming bills, forecast
  - TIDAK ada: vault, API keys, private assets, health score details
- GET /api/v1/shared-view/assets → hanya is_shared = true
- GET /api/v1/shared-view/debts → semua utang (shared by default)
- GET /api/v1/shared-view/bills → semua tagihan
- GET /api/v1/shared-view/monthly-report/:month → closing report

Handler: Gunakan RoleMiddleware("spouse_viewer") dan data filter

═══ FRONTEND ═══

SpouseDashboard.tsx (/spouse — auto-redirect spouse ke sini):
- Simplified dashboard:
  - Total Aset Bersama | Total Utang | Tagihan Bulan Ini
  - Upcoming bills list
  - Forecast saldo akhir bulan
  - Monthly report summary (jika ada closing)
- Label jelas di header: "👀 Melihat sebagai Pasangan — [Nama Owner]"
- TIDAK ada tombol edit/delete/create — semua READ ONLY
- Menu sidebar simplified: hanya Dashboard, Aset, Utang, Tagihan, Laporan
- Tombol setting atau admin TIDAK muncul

Router update:
- Jika role === 'spouse_viewer' → redirect ke /spouse routes
- Spouse tidak bisa akses /transactions, /settings, /vault, /alerts (admin)

PENTING:
- Data private (is_shared = false pada assets) TIDAK BOLEH bocor ke spouse
- Audit log detail TIDAK boleh diakses spouse
- Vault references TIDAK boleh diakses spouse
```

---

## Checklist
- [ ] Login sebagai spouse → redirect ke spouse dashboard
- [ ] Hanya aset bersama yang tampil
- [ ] Utang dan tagihan tampil
- [ ] Forecast tampil
- [ ] TIDAK ada tombol edit/delete/create
- [ ] Menu sidebar simplified
- [ ] Data private TIDAK bocor
- [ ] Label "Melihat sebagai Pasangan" terlihat
